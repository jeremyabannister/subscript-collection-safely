import XCTest
@testable import subscript_collection_safely

final class subscript_collection_safelyTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(subscript_collection_safely().text, "Hello, World!")
    }
}
